<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- LIULIAN baseline theme — no ActionBar, default system bar tinted via Compose. -->
    <style name="Theme.LIULIAN" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

    <style name="Theme.LIULIAN.NoActionBar" parent="Theme.LIULIAN" />
</resources>
